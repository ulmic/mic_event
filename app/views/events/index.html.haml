%table.table-striped
  - @events.each do |event|
    %a{ href: event_path(event) }
      %tr
        = event.title
= paginate @events
- if confirmed_member_signed_in?
  .offset3.span4
    %a.btn{ href: new_event_path } Добавить мероприятие
